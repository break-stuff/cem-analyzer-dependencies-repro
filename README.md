# CEM Analyzer Dependencies Repro

This repository is for testing the `dependencies` feature in a Windows environment.

To get started:

1. Run `npm install`
1. Run `npm run build`
1. Run `npm run analyze`